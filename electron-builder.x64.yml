extends: ./electron-builder.base.yml

extraResources:
  - from: resources/omp.x64
    to: omp
  - from: resources
    to: .
    filter:
      - pi_natives.darwin-x64-*.node

mac:
  target:
    - target: dmg
      arch: [x64]
    - target: zip
      arch: [x64]
